{{.Head}}

// TODO: category description
{{range $typename, $desc := .Types}}
// {{format $desc.Description.PlainText 0}}
type {{camel $typename}} struct {
{{- range $field_name, $field_desc := $desc.Fields}}
	// {{format $field_desc.Description.PlainText 1}}
	{{- if or $field_desc.Required }}
	{{camel $field_name}} {{(index $field_desc.Types 0).GoType}} `json:"{{$field_name}}"`
	{{- else}}
	{{camel $field_name}} {{with (index $field_desc.Types 0)}}{{if not .IsArray}}*{{end}}{{.GoType}}{{end}} `json:"{{$field_name}},omitempty"`
	{{- end}}
{{- end}}
}
{{end}}